import Tile from '../../engine/Tile';
import * as SocketIO from 'socket.io';

class MoveHandler {
    set path(newPath){
        this.path = newPath;
    }
    endTurn() {

    }
    addHeroAction() {

    }
    moveTo(path: Array<Tile>, index: number) {

    }
}
